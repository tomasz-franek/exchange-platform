export enum Features {
  reports = 'reports',
  accounts = 'accounts',
  statistics = 'statistics',
  transactions = 'transactions',
  properties = 'properties',
  messages = 'messages',
  utils = 'utils',
}
